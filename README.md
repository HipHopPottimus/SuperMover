# SupperMover

Brough to you buy Owen and ThePenguin.

```bash
# Install node deps
npm i

# Install python deps (optional - only needed for uDMX)
python3 -m venv .venv
source ./.venv/bin/activate
pip install -r requirements.txt

# Run server
node main.js
```
